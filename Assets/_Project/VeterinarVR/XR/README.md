XR foundation lives here once the editor-side smoke test assets are created.

Immediate next editor tasks:

- Import XR Interaction Toolkit Starter Assets sample.
- Create `S00_XRSmokeTest`.
- Add `XR Origin (VR)`, `XR Interaction Manager`, and `Input Action Manager`.
- Add one world-space UI button and one `XR Grab Interactable` cube.
