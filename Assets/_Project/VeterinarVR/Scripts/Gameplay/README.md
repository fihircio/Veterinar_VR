Gameplay scripts owned by Agent 2 live here.

Current scope:

- `HerdObservationController` for scene-level cow selection flow.
- `CowSelectionTarget` for selectable cow anchors.
- `CowScanController` for scan/decision state.
- `AIProcedureController` for step-driven procedure placeholders.

These scripts are intentionally minimal and should stay scene-safe until prefab and UI wiring is ready.
